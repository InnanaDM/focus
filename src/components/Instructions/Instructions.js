import React from "react";

import "./instructions.css";

const Instructions = () => {
  return (
    <div>
      <p className="flow-text">
        Focus is a game of memory. 24 images will randomly render. Click every
        image once.
      </p>
    </div>
  );
};

export default Instructions;
